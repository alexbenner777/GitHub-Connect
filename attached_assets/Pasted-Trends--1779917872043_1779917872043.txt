Trends — второй итерационный апдейт. Контекст: первый дизайн-апдейт частично внедрён (лендинг, секция партнёров в кабинете, bottom-tabbar, 7 источников монетизации, CSRF cookie — всё ок). Этот промт добивает невыполненные пункты безопасности и UX, исправляет регрессии и добавляет critical-trust-сигналы для Pre-Seed раунда. Порядок выполнения строгий: БЛОК A → B → C → D → E → F. После каждого блока — отдельный коммит. После завершения всех блоков — деплой и сообщение «Готово, можно проверять» со скриншотами по чеклисту из БЛОКА F.

БЛОК A — БЕЗОПАСНОСТЬ (приоритет максимальный, делать первым). A.1. JWT перенести из localStorage (ключ trends_jwt) в httpOnly Secure SameSite=Strict cookie с именем `trends_session`. Из JWT payload удалить email (оставить только userId + exp + iat). Срок жизни access-токена снизить до 7 дней. Реализовать refresh-token endpoint POST /api/auth/refresh с отдельной httpOnly cookie `trends_refresh` (срок 30 дней). На фронте полностью удалить чтение JWT из localStorage, удалить ключ trends_jwt из localStorage при логине новых пользователей. Существующих пользователей с JWT в localStorage — при первом запросе автоматически переводить на cookie-flow (миграция). A.2. Все эндпоинты, требующие авторизации, читают JWT только из cookie, заголовок Authorization больше не поддерживается. A.3. Добавить CSRF-проверку для всех state-changing запросов (POST/PUT/PATCH/DELETE) — проверять заголовок X-CSRF-Token против значения cookie trends_csrf (cookie уже есть, нужна серверная валидация). A.4. На странице /login форму проверить — должна быть POST с CSRF-токеном, никакой передачи пароля через URL. A.5. На странице смены wallet в Настройках добавить обязательное email-подтверждение: при изменении wallet-адреса отправляется письмо с 6-значным кодом, без ввода кода адрес НЕ меняется в БД. История изменений wallet (последние 5: дата, старый адрес, новый адрес) показывается под формой. A.6. Console-warning от Radix DialogContent про aria-describedby — починить во всех Dialog-компонентах.

БЛОК B — РЕГРЕССИИ И ИСПРАВЛЕНИЯ ТЕКУЩЕГО СОСТОЯНИЯ. B.1. Конфликт цифр раунда на лендинге: сейчас в hero «$2 000 000 целевой объём», а в секции «Инвестиционный спринт» Pre-seed Alpha $500 000 + Seed Global Scale $1 500 000. Унифицировать: оставить ОДИН Pre-Seed раунд $2 000 000, внутри разбить на «Round 1 (активен, +30% бонус) — $500 000» и «Round 2 — $1 500 000» (БЕЗ слова «Seed», это всё ещё Pre-Seed). Title документа, hero, секция «Инвестиционный спринт», FAQ, /legal/offer — везде привести к единой формулировке. B.2. Bottom-tabbar в кабинете: показывать только на ≤768px. На десктопе (>768px) скрыть, вместо него вернуть/добавить side-nav слева ИЛИ top-tabs под шапкой. Сейчас bottom-tabbar показывается на всех ширинах — регрессия. B.3. Кнопка «+ Вложить» в шапке кабинета в правом верхнем углу: либо переместить её внутрь карточки «Инвестировано» (как primary CTA там), либо сделать floating action button в правом нижнем углу на мобайле + кнопкой в десктопном sidebar. Сейчас она наезжает на logout-иконку. B.4. График «Ваш RevShare при разных DAU» в кабинете на главной — сейчас показывается ТОЛЬКО если у юзера есть инвестиция. Изменить: показывать ВСЕГДА, даже при $0 инвестиций, как preview — «Вот сколько вы будете получать, если вложите [Starter $250 / Partner $1000 / ...]» с переключателем пакетов сверху графика. Это конвертит. B.5. H1 на лендинге для мобайла: применить font-size: clamp(28px, 8vw, 56px); line-height: 1.05; letter-spacing: -0.02em; word-break: keep-all. Сейчас «Trends — первый Reels внутри Telegram» переносится по одному слову — недопустимо. B.6. Пустоты между секциями лендинга после блока «Telegram получает...»: сократить vertical padding на 30-40% (с py-32 до py-20 или с margin-top 200px до 120px). B.7. Skeleton вместо текста «Загрузка кабинета»: при загрузке /cabinet показывать <Skeleton> блоки той же формы, что и итоговый layout (4 KPI-карточки + история операций) — не текст-loader. Использовать shadcn skeleton или ручной с pulse-анимацией.

БЛОК C — ЛИЧНЫЙ КАБИНЕТ, добиваем premium-клуб. C.1. Шапка кабинета: добавить аватар (генерируется из инициалов имени, фон — детерминированный градиент по hash(userId)), имя пользователя, email под именем мелким шрифтом, бейдж тира справа от имени. Логика бейджа тира: если есть активный пакет Co-Investor ($100k) — «Co-Investor» с золотым glow и анимацией pulse; если Founder ($25k) — «Founder» серебряный; если Premium ($5k) — «Premium» violet; если Partner ($1k) — «Partner» cyan; если Starter ($250) — «Starter» серый; если 0 инвестиций — «Гость» mute. Под бейджем — мелкий текст «инвестор с [месяц год регистрации]». C.2. KPI-плашки (Инвестировано/RevShare/MLM/Партнёров): каждую переработать в <MetricCard> — большая цифра, ПОД ней дельта за 7 дней («+$0.00 за неделю» или «—» если нет данных), встроенный sparkline 30 дней (recharts mini), для нулевых метрик — embedded CTA вместо тоскливого «$0»: «Инвестировано $0» → кнопка «Сделать первую инвестицию →», «RevShare $0» → подпись «Появится после первой инвестиции», «MLM $0» → «Пригласите первого →», «Партнёров 0» → «Скопировать реф-ссылку →» (уже есть только в Партнёрах, добавить и в Обзор). C.3. Empty-state для нового юзера на главной кабинета: большой topblock «Добро пожаловать в Trends! 3 шага до первого RevShare» с прогресс-баром: (1) Сделать первую инвестицию [CTA «Выбрать пакет»] / (2) Привязать кошелёк USDT [CTA «Настройки → Wallet»] / (3) Пригласить первого партнёра [CTA «Скопировать ссылку»]. Каждый шаг с checkbox-state (выполнено / в процессе / не начато). Блок исчезает, когда все 3 шага выполнены.

БЛОК D — РАЗДЕЛ «ПЛАТФОРМА» И ПРОГРЕСС РАУНДА. D.1. Полностью удалить заглушку «Данные платформы скоро появятся». Вместо неё построить дашборд из 4 секций: (1) Live-метрики в виде 4 больших MetricCard — DAU, MAU, Видео загружено за неделю, Активных авторов. Данные берутся из реальной БД или, если данных пока мало, показывать минимальные числа с пометкой «MVP-период». (2) Прогресс раунда — большой блок с прогресс-баром «Собрано $X из $2 000 000» (X = SUM(amount) FROM investments WHERE status='confirmed' AND round='round1'), counter «N инвесторов вступили» (COUNT DISTINCT userId), countdown до окончания Round 1 (дата окончания хранится в env или config, например ROUND1_END_DATE=2026-09-30). (3) Roadmap timeline по горизонтали: Q1 2026 ✓ / Q2 2026 (in progress) / Q3 2026 / Q4 2026 — каждая колонка с галочками milestones. (4) News feed — 5 последних апдейтов команды (новая таблица platform_updates: id, date, title, body markdown, опубликовано/черновик), на главной кабинета показывать последний апдейт preview с CTA «Все новости». D.2. Тот же блок «Прогресс раунда» (D.1.2) добавить и на лендинг — сразу над секцией пакетов: большой progress-bar + counter + countdown. Это критичный driver of urgency, его сейчас нигде нет.

БЛОК E — ЛЕНДИНГ, доверие и эмоция. E.1. Team-секция: добавить новый блок «Команда Trends» с 4-6 карточками членов команды. Поля карточки: фото (или mock-аватар), имя или роль (если имя пока нельзя — «Founder & CEO», «CTO», «Head of Product», «Head of Marketing», «AI/ML Lead», «Advisor»), 1-2 строки опыта, ссылки LinkedIn/Telegram (опционально). Карточки в grid 3 в ряд на десктопе, 1 в ряд на мобайле. Без team-секции pre-seed раунд не вызывает доверия. E.2. Trust-сигналы в hero: под основными CTA («Инвестировать сейчас» / «Войти в кабинет») добавить тонкую row с микро-доказательствами: иконка «🔒 Защищённый платёж USDT/TON» · «✓ Договор-оферта» · «👥 N инвесторов в Round 1» (N — реальный счётчик из БД, минимум показывать 1, если 1). Высота row — 24px, цвет text-muted, разделители «·». E.3. Видео-pitch блок: добавить секцию «60 секунд о Trends» с embedded video (YouTube/Vimeo через lite-youtube-embed для производительности, без iframe сразу). Если видео ещё нет — поставить placeholder «Видео-pitch скоро» с фоном hero-mockup и CTA «Подписаться на канал команды». Разместить блок между «Что такое Trends» и «Огромный рынок пуст». E.4. Social proof секция: «Что говорят о Trends» — 3 testimonial-карточки. Источник цитат на выбор: первые юзеры MVP (с их разрешения), Telegram-эксперты, упоминания в медиа. Если ничего пока нет — показывать только 1 карточку «Будь первым, кто оценит» с CTA «Открыть MVP». Не выдумывать фейковые цитаты от несуществующих людей. E.5. Hero: добавить под H1 (но над описанием) мелкую строку «47 инвесторов уже в Round 1 · Собрано $X из $2M» — это live-цифры из БД, обновляются раз в минуту. Если инвесторов меньше 10 — пока скрыть, чтобы не показывать слабые цифры.

БЛОК F — ВЕРИФИКАЦИЯ (обязательно после внедрения). Сделать скриншоты и приложить к ответу: (1) DevTools Application → Cookies: должна быть trends_session с пометками HttpOnly + Secure + SameSite=Strict. localStorage пустой по ключу trends_jwt. (2) Hero лендинга на десктопе 1440px — с trust-row под CTA и live-счётчиком инвесторов. (3) Hero лендинга на мобайле 390px — H1 без переноса по одному слову, читается на 2-3 строки максимум. (4) Секция Team на лендинге. (5) Блок «Прогресс раунда» на лендинге над пакетами. (6) Шапка кабинета: аватар + имя + бейдж тира + дата вступления. (7) KPI-плашки кабинета: с дельтами, спарклайнами и CTA для нулевых метрик. (8) Раздел «Платформа» в кабинете: 4 live-метрики + прогресс + roadmap + news feed (вместо заглушки). (9) Bottom-tabbar только на мобайле, на десктопе — side-nav или top-tabs. (10) График RevShare при разных DAU в кабинете показывается даже при $0 инвестиций как preview с переключателем пакетов. Также подтвердить текстом: ✅ JWT в httpOnly cookie, в localStorage отсутствует; ✅ Email-подтверждение при смене wallet работает (отправка кода); ✅ CSRF-токен валидируется на сервере для POST/PUT/PATCH/DELETE; ✅ Цифры раунда унифицированы ($2M Pre-Seed = Round 1 $500k + Round 2 $1.5M, нет конфликтов); ✅ H1 на мобайле не переносится по одному слову; ✅ Lighthouse Mobile Performance ≥ 80, Accessibility ≥ 95; ✅ Радикс DialogContent warning в консоли отсутствует. Порядок коммитов: chore(security): jwt to httpOnly cookies + csrf validation → fix(regressions): round numbers unify + responsive nav + mobile h1 → feat(cabinet): user header + metric cards with deltas + onboarding empty-state → feat(platform): live dashboard + round progress + roadmap + news → feat(landing): team + trust signals + video pitch + social proof. После всего — деплой и сообщение «Готово, можно проверять».